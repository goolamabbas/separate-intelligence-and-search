const fs=require('node:fs'),path=require('node:path');
const {Marked}=require('marked');
const root=path.resolve(__dirname,'..'),base='https://goolamabbas.github.io/separate-intelligence-and-search/';
const dir=path.join(root,'docs/case-studies/exa-ultra'),out=path.join(root,'_site/guide/exa-ultra-case-study');
const request=JSON.parse(fs.readFileSync(path.join(dir,'request.json'))),response=JSON.parse(fs.readFileSync(path.join(dir,'response.json'))),s=response.output.structured;
const source=fs.readFileSync(path.join(root,'docs/exa-ultra-case-study.md'),'utf8');
const esc=x=>String(x).replaceAll('&','&amp;').replaceAll('<','&lt;').replaceAll('>','&gt;').replaceAll('"','&quot;');
const slug=x=>x.toLowerCase().replace(/[^\p{L}\p{N}_\- ]/gu,'').replaceAll(' ','-');
const md=new Marked();md.use({renderer:{heading({depth,text,tokens}){return `<h${depth} id="${slug(text)}">${this.parser.parseInline(tokens)}</h${depth}>`;}}});
const para=x=>md.parseInline(esc(x));
const labels={category:'Category',canonical_url:'Primary link',eligibility_date_and_evidence:'Release or update evidence',measures:'What it measures',task_data:'Task data',evaluation_code:'Evaluation code',judging_criteria:'Judging criteria',reproducibility_and_barriers:'Reproducibility and barriers',reported_results_provenance:'Who reported the results'};
const links=e=>`<ul>${e.map(x=>`<li><a href="${esc(x.url)}">${esc(x.url)}</a><p>${para(x.supports)}</p></li>`).join('')}</ul>`;
const records=s.benchmarks.map((b,i)=>`<details class="record" id="benchmark-${i+1}"><summary><span>${i+1}. ${esc(b.name)}</span><small>${esc(b.category)}</small></summary><div class="record-body"><p class="attribution">EXA-GENERATED RECORD · NOT INDEPENDENTLY VERIFIED</p><a href="#benchmark-${i+1}">Link to this record</a><dl>${Object.entries(labels).map(([k,l])=>`<dt>${l}</dt><dd data-field="${k}">${k==='canonical_url'?`<a href="${esc(b[k])}">${esc(b[k])}</a>`:para(b[k])}</dd>`).join('')}</dl><h4>Evidence supplied by Exa</h4>${links(b.evidence)}</div></details>`).join('');
const adjacent=s.adjacent_or_uncertain.map((b,i)=>`<details class="record" id="adjacent-${i+1}"><summary>${esc(b.name)}</summary><div class="record-body"><p class="attribution">EXA-GENERATED CLASSIFICATION · NOT INDEPENDENTLY VERIFIED</p><a href="${esc(b.url)}">${esc(b.url)}</a><p>${para(b.reason)}</p></div></details>`).join('');
const provider=`<div class="provider"><p class="attribution">EXA OUTPUT · WORDING PRESERVED</p><h3>Exa's summary</h3><p>${para(s.summary)}</p><details><summary>Exa's scope interpretation</summary><p>${para(s.scope)}</p></details><details><summary>Exa's stated coverage limitations</summary><ul>${s.coverage_gaps.map(x=>`<li>${para(x)}</li>`).join('')}</ul></details></div><h3 id="catalogue">Explore Exa's 54 catalogue records</h3><p>These are the entries Exa classified as qualifying. Search covers all record fields; it does not score or validate them.</p><label class="search-label" for="catalogue-search">Find a name, topic, or phrase</label><input type="search" id="catalogue-search" placeholder="Try citation, list, or reproducibility"><p id="catalogue-count" role="status" aria-live="polite">54 of 54 catalogue records</p><div id="catalogue-records">${records}</div><p id="no-results" hidden>No matching catalogue records. Clear the search to see all entries.</p><details class="adjacent-group"><summary>30 adjacent, uncertain, or excluded entries — Exa's classifications</summary><p>The catalogue search above does not filter this separate group.</p>${adjacent}</details>`;
const originals=`<details id="exact-question"><summary>Exact research question sent to Exa</summary><p class="attribution">ASSISTANT-AUTHORED ASSIGNMENT</p><pre>${esc(request.query)}</pre></details><details><summary>Additional instructions sent to Exa</summary><p class="attribution">ASSISTANT-AUTHORED INSTRUCTIONS</p><pre>${esc(request.systemPrompt)}</pre></details><details><summary>Requested output structure</summary><p class="attribution">ASSISTANT-DESIGNED SCHEMA</p><pre>${esc(JSON.stringify(request.outputSchema,null,2))}</pre></details><ul class="downloads"><li><a href="request.json" download>Download exact request (JSON)</a></li><li><a href="response.json" download>Download Exa response (JSON; run identifier removed)</a></li><li><a href="provenance.json" download>Download provenance and file hashes (JSON)</a></li></ul>`;
let article=md.parse(source.replace(/^# .*\n/,''));
article=article.replace('<h2 id="inspect-the-original-materials">',provider+'<h2 id="inspect-the-original-materials">');
article=article.replace('<h2 id="what-to-take-away">',originals+'<h2 id="what-to-take-away">');
const headings=[...source.matchAll(/^## (.+)$/gm)].map(m=>m[1]);const toc=headings.map(x=>`<a href="#${slug(x)}">${esc(x)}</a>`).join('');
fs.mkdirSync(out,{recursive:true});
fs.writeFileSync(path.join(out,'index.html'),`<!doctype html><html lang="en"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>Inside one Exa Ultra research run</title><meta name="description" content="Inspect one Exa Ultra research run: the assistant's assignment, Exa's findings, and the assistant's assessment, clearly separated."><link rel="canonical" href="${base}guide/exa-ultra-case-study/"><link rel="stylesheet" href="guide.css"><link rel="stylesheet" href="case-study.css"></head><body><a class="skip" href="#main">Skip to case study</a><header class="header"><div class="header-inner"><a class="brand" href="${base}">Separate intelligence & search</a><nav aria-label="Site navigation"><a href="${base}guide/">Research guide</a><a href="https://github.com/goolamabbas/separate-intelligence-and-search/blob/main/docs/exa-ultra-case-study.md">View page source</a></nav></div></header><div class="layout"><aside class="sidebar"><nav class="guide-nav" aria-label="Guide pages"><p class="nav-label">WORKED EXAMPLE</p><a href="${base}guide/">Guide overview</a><a href="${base}guide/choosing-providers/#when-agent-ultra-is-worth-it">When Ultra is worth it</a><a class="selected" href="#main" aria-current="page">Inside one Ultra run</a></nav><nav class="toc" aria-label="On this page">${toc}</nav></aside><main id="main"><div class="breadcrumb"><a href="${base}guide/">Research guide</a> / Worked example</div><p class="eyebrow">ONE RUN · THREE CONTRIBUTIONS</p><h1>Inside one Exa Ultra research run</h1><p class="subtitle">The assignment, the findings, and the presentation—separated so you can judge them.</p><div class="metrics"><span><b>$11.66</b> reported Exa cost</span><span><b>92</b> reported searches</span><span><b>54 + 30</b> returned records</span></div><div class="notice"><strong>Provider-generated findings; no independent source audit.</strong> One completed run, no comparison run. Search counts and output length do not measure quality.</div><p class="attribution">PAGE NARRATIVE: ASSISTANT-AUTHORED · EXA SECTIONS LABELLED BELOW</p><nav class="shortcuts" aria-label="Reading options"><a href="#who-contributed-what">Understand the contributions</a><a href="#catalogue">Explore the catalogue</a><a href="#inspect-the-original-materials">Inspect the originals</a></nav><article>${article}</article><footer class="article-footer"><a href="${base}guide/">Back to the research guide</a><a href="${base}guide/choosing-providers/#when-agent-ultra-is-worth-it">Read about Ultra</a></footer></main></div><script src="case-study.js"></script></body></html>`);
for(const name of ['request.json','response.json','provenance.json'])fs.copyFileSync(path.join(dir,name),path.join(out,name));
for(const name of ['guide.css','case-study.css','case-study.js'])fs.copyFileSync(path.join(root,'site',name),path.join(out,name));
console.log(`Built Ultra case study: ${s.benchmarks.length} records, ${s.adjacent_or_uncertain.length} adjacent entries.`);
